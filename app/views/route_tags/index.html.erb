<h1>Listing route_tags</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @route_tags.each do |route_tag| %>
  <tr>
    <td><%= link_to 'Show', route_tag %></td>
    <td><%= link_to 'Edit', edit_route_tag_path(route_tag) %></td>
    <td><%= link_to 'Destroy', route_tag, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Route tag', new_route_tag_path %>
