<%= form_for(@route_tag) do |f| %>
  <% if @route_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route_tag.errors.count, "error") %> prohibited this route_tag from being saved:</h2>

      <ul>
      <% @route_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
