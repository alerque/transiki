<h1>Listing point_tags</h1>

<table>
  <tr>
    <th>Key</th>
    <th>Value</th>
    <th>Point</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @point_tags.each do |point_tag| %>
  <tr>
    <td><%= point_tag.key %></td>
    <td><%= point_tag.value %></td>
    <td><%= point_tag.point_id %></td>
    <td><%= link_to 'Show', point_tag %></td>
    <td><%= link_to 'Edit', edit_point_tag_path(point_tag) %></td>
    <td><%= link_to 'Destroy', point_tag, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Point tag', new_point_tag_path %>
