<%= form_for(@old_route_point) do |f| %>
  <% if @old_route_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@old_route_point.errors.count, "error") %> prohibited this old_route_point from being saved:</h2>

      <ul>
      <% @old_route_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
